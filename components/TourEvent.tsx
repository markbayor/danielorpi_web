import React from "react";
import {TourEventParams} from "../common/types";

const TourEvent = ({location, date, lineup, ticketLink}: TourEventParams) => {
  return (
    <div className='tour_event'>
      
    </div>
  )
}

export {TourEvent}